# =============================================================================
# SaaS Complete - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!

# =============================================================================
# ENVIRONMENT
# =============================================================================
# Set to 'development' for local development, 'production' for deployment
NODE_ENV=development

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/{your-project}/settings/api

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anonymous key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role key (keep secret! server-side only)
SUPABASE_SERVICE_ROLE_KEY=
# =============================================================================
# DATABASE
# =============================================================================
# Get this from Supabase project settings:
# https://app.supabase.com/project/{your-project}/settings/database
# Use the connection string provided by Supabase (pooler URL works fine with Drizzle)

DATABASE_URL=

# =============================================================================
# STRIPE CONFIGURATION
# =============================================================================
# Get these from your Stripe dashboard:
# https://dashboard.stripe.com/test/apikeys (for test mode)
# https://dashboard.stripe.com/apikeys (for live mode)

# Stripe secret key (keep secret! server-side only)
STRIPE_SECRET_KEY=

# Stripe publishable key (safe to expose in browser)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Stripe webhook secret (get this after setting up webhook endpoint)
# Run: pnpm stripe:webhook:setup to automatically create and set this
STRIPE_WEBHOOK_SECRET=

# =============================================================================
# APP CONFIGURATION
# =============================================================================
# Your application's base URL
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_BASE_URL=

# Support email address (displayed on website and in contact forms)
NEXT_PUBLIC_SUPPORT_EMAIL=support@yourdomain.com

# Social Media URLs (optional - leave empty to hide from schema.org markup)
NEXT_PUBLIC_TWITTER_URL=
NEXT_PUBLIC_LINKEDIN_URL=
NEXT_PUBLIC_GITHUB_URL=

# =============================================================================
# EMAIL CONFIGURATION (Resend)
# =============================================================================
# Get your API key from: https://resend.com/api-keys
# Sign up at: https://resend.com

# Resend API key
RESEND_API_KEY=

# Email address to send FROM (must be verified domain in Resend)
RESEND_FROM_EMAIL=

# Email address to send TO (for contact form submissions)
RESEND_TO_EMAIL=

# =============================================================================
# AI CONFIGURATION (OpenRouter)
# =============================================================================
# Get your API key from: https://openrouter.ai/keys
# Sign up at: https://openrouter.ai

# OpenRouter API key
OPENROUTER_API_KEY=

# AI model temperature (0.0 = deterministic, 1.0 = creative)
# Default: 0.7 (balanced)
AI_TEMPERATURE=0.7

# =============================================================================
# CRON JOBS (Vercel)
# =============================================================================
# Required for scheduled jobs (e.g., monthly token reset)
# Generate a secure random string: openssl rand -base64 32
# Add this to Vercel project environment variables as well
CRON_SECRET=

# =============================================================================
# SETUP WIZARD
# =============================================================================
# Set to 'true' after completing the setup wizard
# This variable is automatically set by the setup wizard on completion
# You can also manually set it to skip the setup wizard
SETUP_COMPLETE=
